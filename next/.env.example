# 前端环境变量示例。复制为 .env.local 后按需修改。

# API 基础路径。开发环境留空（注释掉）即可：
#   - api.ts 的 resolveApiBase() 会自动按当前页面 host 拼出后端地址
#   - 这样用 localhost / 127.0.0.1 / 局域网 IP 访问前端时，cookie 落在同 host，middleware 能正常读取
#
# 如需指向独立后端（如联调远端），取消注释并填写完整地址：
# NEXT_PUBLIC_API_BASE="http://192.168.1.100:3721/api/v1"

# 生产环境通过 Nginx 代理，路径为 /api/v1
# NEXT_PUBLIC_API_BASE="/api/v1"
